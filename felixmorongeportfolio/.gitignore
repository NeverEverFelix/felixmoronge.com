# --- Node.js (Vite + React) ---
node_modules/
dist/
.vite/
*.log

# --- Env files (secrets) ---
.env
rag-embed-py/.env
rag-api-go/.env

# --- Terraform ---
terraform/
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
crash.log
terraform.tfvars

# --- Kubernetes ---
*.yaml~
*.backup
kubeconfig
.kube/
*.kubeconfig

# --- Docker ---
docker-compose.override.yml
docker-compose.local.yml
*.tar
*.tgz
*.img
*.iso
*.zip

# --- Jenkins ---
secrets/
var/jenkins_home/
jenkins_home/
*.jenkinsfile~  # old backup
*.secret
config.xml
credentials.xml

# --- VS Code ---
.vscode/

# --- macOS ---
.DS_Store

# --- System junk ---
Thumbs.db
ehthumbs.db
desktop.ini

# --- Certbot / SSL ---
/etc/letsencrypt/
certbot-auto
*.pem
*.key

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*

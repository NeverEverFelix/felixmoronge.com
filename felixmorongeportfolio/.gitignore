# Node.js (Vite + React)
node_modules/
dist/
.env
.vite/

# macOS
.DS_Store

# Docker
*.log
docker-compose.override.yml
docker-compose.local.yml

# Kubernetes
*.yaml~
*.backup
kubeconfig
.kube/
*.kubeconfig

# Jenkins
secrets/
var/jenkins_home/
jenkins_home/
*.jenkinsfile~  # old backup
*.secret
config.xml
credentials.xml

# Terraform (if used)
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
crash.log
terraform.tfvars

# VS Code
.vscode/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Certbot / SSL
/etc/letsencrypt/
certbot-auto
*.pem
*.key

# System junk
Thumbs.db
ehthumbs.db
desktop.ini

# Optional: prevent pushing large artifacts or Docker images
*.tar
*.tgz
*.img
*.iso
*.zip
